# Loaded automatically by `next dev` (local development). Points the frontend
# at your LOCAL backend. Not used by production builds.
NEXT_PUBLIC_API_URL=http://localhost:5000
NEXT_PUBLIC_IMAGE_URL=http://localhost:5000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
